<h1>Wind Call</h1>
<h2>Trouve ton prochain spot de Kite Surf !</h2>

<p>Découvre les plus beaux spots de Kite
Surf en fonction de tes critères  à travers
la France !</p>

<button class=" button-alert">
  <i class="fa fa-bell"></i>
  M'alerter
</button>
<br>
<br>

<%= form_with url: spots_path, method: "get", data: { controller: "double-slider"} do |f| %>
  <p>Séléctionne la DIFFICULTE de ton spot</p>
  <div class="choose-difficulty-spot">

    <div class="first-two-div">
      <%= f.radio_button :difficulty, "Débutant", id: "deb" %>
      <%= f.label :difficulty, "Débutant", class:"difficulty-spot", for: "deb" %>

      <%= f.radio_button :difficulty, "Intermédiaire", id: "int" %>
      <%= f.label :difficulty, "Intermédiaire", class:"difficulty-spot", for: "int" %>
    </div>

    <div class="two-last-div">
      <%= f.radio_button :difficulty, "Confirmé", id: "con" %>
      <%= f.label :difficulty, "Confirmé", class:"difficulty-spot", for: "con" %>

      <%= f.radio_button :difficulty, "Expert", id: "exp" %>
      <%= f.label :difficulty, "Expert", class:"difficulty-spot", for: "exp" %>
    </div>
  </div>

  <p>Selectionne la FORCE du vent !</p>

  <div class="double-slider">
    <span class="value" data-double-slider-target="rangeOne"></span>
    <div class="slider-container">
      <div data-double-slider-target="sliderBar" class="slider-bar"></div>
      <%= f.range_field :wind_min, value: params[:wind_min] || "15", min: "0", max: "60", data: { double_slider_target: "sliderOne", action: "input->double-slider#slideOne"} %>
      <%= f.range_field :wind_max, value: params[:wind_max] || "30", min: "0", max: "60", data: { double_slider_target: "sliderTwo", action: "input->double-slider#slideTwo"} %>
    </div>
    <span class="value" data-double-slider-target="rangeTwo"></span>

  </div>

  <p>Quand veux-tu aller Kite-Surfer</p>

    <div class="first-two-div">
      <%= f.radio_button :time, "today", id: "tod" %>
      <%= f.label :time, "Aujourd'hui", class:"when-kite-spot", for: "tod" %>

      <%= f.radio_button :time, "tomorrow", id: "tom" %>
      <%= f.label :time, "Demain", class:"when-kite-spot", for: "tom" %>

      <%= f.radio_button :time, "after", id: "after" %>
      <%= f.label :time, "Après-demain", class:"when-kite-spot", for: "after" %>
    </div>


    <div class="form_geolocation">
      <%= f.text_field :address, placeholder: "address" %>
      <%= f.text_field :perimeter, placeholder: "> 20km" %>
    </div>
    <%= f.button "search" %>
<% end %>
