<!DOCTYPE html>
<html>
  <head>
    <title>WindCall</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap');
    </style>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <meta name="description" content="Grâce à Wind Call, vous allez pouvoir trouver les meilleurs spots de Kite Surf en fonction de vos propres critères !">
    <meta property="og:title" content="Wind Call - Le meilleur site pour les Kites Surfers !">
    <meta property="og:image" content="picto.jpg">
    <meta property="og:site_name" content="Le Wagon"/>

    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">

    <%= render 'shared/ios_pwa_links' %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
     <main>
        <%= yield %>
     </main>
    <%= turbo_frame_tag "modal" %>
    <%# <%= render 'shared/footer' %>

  </body>
</html>
